<%= render 'shared/header' %>

<div class="container">
	<h1>New Post</h1>

	<%= bootstrap_form_for :post, html: { multipart: true }, url: posts_path do |f| %>
        <%= render 'posts/form', f: f %>
	<% end %>

	<div class="clearfix">
		<%= link_to 'Return to All Posts', root_path, class: 'btn btn-secondary float-right' %>
	</div>

</div>